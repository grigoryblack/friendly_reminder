# 🚀 Деплой Friendly Reminder на Render

## 📋 Подготовка

### 1. Проверьте подключение к базе данных

Ваша БД уже создана на Render:
```
postgresql://root:80gnIBVDbbfCXEUEPLo0PKDtNyKsMQhB@dpg-d4fgn615pdvs73ag1upg-a/friendly_reminder
```

### 2. Сгенерируйте NEXTAUTH_SECRET

Выполните команду локально:
```bash
openssl rand -base64 32
```

Сохраните полученную строку - она понадобится для настройки переменных окружения.

---

## 🔧 Деплой на Render

### Вариант 1: Через Render Dashboard (Рекомендуется)

#### Шаг 1: Создайте новый Web Service

1. Зайдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub/GitLab репозиторий

#### Шаг 2: Настройте сервис

**Основные настройки:**
- **Name:** `friendly-reminder`
- **Region:** `Frankfurt` (или ближайший к вам)
- **Branch:** `main` (или ваша основная ветка)
- **Runtime:** `Docker`
- **Plan:** `Free` (или платный план)

#### Шаг 3: Настройте Environment Variables

Добавьте следующие переменные окружения:

```bash
# Database (уже есть у вас)
DATABASE_URL=postgresql://root:80gnIBVDbbfCXEUEPLo0PKDtNyKsMQhB@dpg-d4fgn615pdvs73ag1upg-a/friendly_reminder

# NextAuth (ОБЯЗАТЕЛЬНО!)
NEXTAUTH_URL=https://your-app-name.onrender.com
NEXTAUTH_SECRET=<ваш_сгенерированный_секрет>

# SMTP для отправки писем
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=drz2002@yandex.ru
SMTP_PASSWORD=<ваш_пароль_приложения>
SMTP_FROM=noreply@friendly-reminder.com

# MEGA для загрузки изображений
MEGA_EMAIL=drz2002@yandex.ru
MEGA_PASSWORD=grigory2002

# Дополнительно
NODE_ENV=production
RUN_SEED=true  # Установите true для первого деплоя, потом false
```

#### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Render автоматически:
   - Склонирует репозиторий
   - Соберет Docker образ
   - Запустит миграции БД
   - Запустит приложение

3. Дождитесь завершения деплоя (5-10 минут)

---

### Вариант 2: Через render.yaml (Blueprint)

#### Шаг 1: Загрузите код в Git

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

#### Шаг 2: Создайте Blueprint на Render

1. Зайдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите репозиторий
4. Render автоматически обнаружит `render.yaml`

#### Шаг 3: Настройте секретные переменные

В Render Dashboard установите вручную:
- `NEXTAUTH_URL` - ваш URL приложения
- `NEXTAUTH_SECRET` - сгенерированный секрет
- `SMTP_PASSWORD` - пароль от email
- `MEGA_PASSWORD` - пароль от MEGA

---

## 🔐 Настройка SMTP (Gmail)

Для отправки писем через Gmail:

1. Включите **2-Factor Authentication** в Google аккаунте
2. Создайте **App Password**:
   - Google Account → Security → 2-Step Verification → App passwords
   - Выберите "Mail" и "Other (Custom name)"
   - Скопируйте сгенерированный пароль
3. Используйте этот пароль в `SMTP_PASSWORD`

---

## 🗄️ Миграции базы данных

### Автоматические миграции

При каждом деплое автоматически выполняются:
```bash
npx prisma migrate deploy
```

### Ручные миграции (если нужно)

Подключитесь к БД через Render Shell:
```bash
npx prisma migrate deploy
npx prisma db seed  # Если нужно заполнить тестовыми данными
```

---

## 🌱 Заполнение базы данных

### Первый деплой

Установите переменную окружения:
```bash
RUN_SEED=true
```

Это создаст тестовые аккаунты:
- **Admin:** admin@example.com / admin123
- **Teacher:** teacher@example.com / teacher123
- **Student:** student@example.com / student123

### Последующие деплои

Измените на:
```bash
RUN_SEED=false
```

---

## 🔍 Проверка деплоя

### Health Check

Откройте в браузере:
```
https://your-app-name.onrender.com/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### Логи

Смотрите логи в Render Dashboard:
- **Logs** → **Deploy Logs** - логи сборки
- **Logs** → **Service Logs** - логи приложения

---

## 🐛 Troubleshooting

### Проблема: "Database connection failed"

**Решение:**
1. Проверьте правильность `DATABASE_URL`
2. Убедитесь, что БД запущена на Render
3. Проверьте firewall правила

### Проблема: "Prisma migration failed"

**Решение:**
```bash
# Подключитесь к Render Shell
npx prisma migrate reset --force
npx prisma migrate deploy
```

### Проблема: "NEXTAUTH_SECRET not set"

**Решение:**
1. Сгенерируйте секрет: `openssl rand -base64 32`
2. Добавьте в Environment Variables на Render
3. Перезапустите сервис

### Проблема: "Build failed"

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что все зависимости установлены
3. Проверьте `package.json` и `package-lock.json`

---

## 🔄 Обновление приложения

### Автоматический деплой

Render автоматически деплоит при push в main ветку:
```bash
git add .
git commit -m "Update feature"
git push origin main
```

### Ручной деплой

В Render Dashboard:
1. Выберите ваш сервис
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

---

## 📊 Мониторинг

### Метрики

В Render Dashboard доступны:
- CPU usage
- Memory usage
- Response time
- Request count

### Алерты

Настройте уведомления:
- Email alerts при падении сервиса
- Slack/Discord интеграция

---

## 💰 Стоимость

### Free Plan
- ✅ 750 часов в месяц
- ✅ Автоматический sleep после 15 минут неактивности
- ✅ 512 MB RAM
- ⚠️ Холодный старт (может быть медленным)

### Starter Plan ($7/месяц)
- ✅ Без sleep режима
- ✅ 512 MB RAM
- ✅ Быстрый отклик

### Standard Plan ($25/месяц)
- ✅ 2 GB RAM
- ✅ Больше CPU
- ✅ Priority support

---

## 🔗 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Docker Guide](https://render.com/docs/docker)
- [Prisma Deployment Guide](https://www.prisma.io/docs/guides/deployment)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

---

## ✅ Чеклист деплоя

- [ ] База данных создана на Render
- [ ] `DATABASE_URL` настроен
- [ ] `NEXTAUTH_SECRET` сгенерирован и установлен
- [ ] `NEXTAUTH_URL` установлен (с https://)
- [ ] SMTP настроен (Gmail App Password)
- [ ] MEGA учетные данные установлены
- [ ] `RUN_SEED=true` для первого деплоя
- [ ] Код загружен в Git репозиторий
- [ ] Web Service создан на Render
- [ ] Деплой завершен успешно
- [ ] Health check работает
- [ ] Можно войти в приложение
- [ ] `RUN_SEED=false` после первого деплоя

---

## 🎉 Готово!

Ваше приложение должно быть доступно по адресу:
```
https://your-app-name.onrender.com
```

Войдите с тестовыми учетными данными или создайте новый аккаунт.
