# 🚀 Quick Start Guide

## Быстрый запуск для разработки

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка окружения
```bash
# Скопируйте файл с переменными окружения
cp .env.example .env.development.local

# Отредактируйте .env.development.local и укажите:
# - DATABASE_URL (PostgreSQL)
# - NEXTAUTH_SECRET (любая случайная строка)
# - SMTP настройки (для тестирования можно использовать Mailtrap)
# - MEGA креды (для загрузки файлов)
```

### 3. Настройка базы данных

#### Вариант A: Локальный PostgreSQL
```bash
# Установите PostgreSQL и создайте базу данных
createdb friendly_reminder_dev

# Запустите миграции
npm run db:migrate

# Заполните тестовыми данными
npm run db:seed
```

#### Вариант B: Docker PostgreSQL
```bash
# Запустите PostgreSQL в Docker
docker run --name postgres-dev \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=friendly_reminder_dev \
  -p 5432:5432 -d postgres:15

# Обновите DATABASE_URL в .env.development.local:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/friendly_reminder_dev"

# Запустите миграции и seed
npm run db:migrate
npm run db:seed
```

### 4. Запуск приложения
```bash
npm run dev
```

Откройте http://localhost:3000

## 🔑 Тестовые аккаунты

После выполнения `npm run db:seed` будут доступны:

- **Admin**: admin@friendly-reminder.com / admin123
- **Teacher**: teacher@friendly-reminder.com / teacher123  
- **Student**: student@friendly-reminder.com / student123

## 📱 Debug страницы

- `/` - Главная страница с навигацией
- `/auth/login` - Вход в систему
- `/auth/register` - Регистрация
- `/courses` - Список курсов
- `/upload-test` - Тест загрузки файлов в MEGA

## 🐳 Production (Docker)

```bash
# Настройте production переменные
cp .env.example .env.production
# Отредактируйте .env.production

# Запустите все сервисы
docker compose up --build -d

# Приложение будет доступно на http://localhost:3000
# PgAdmin на http://localhost:5050
```

## 🔧 Полезные команды

```bash
# Генерация Prisma клиента
npm run db:generate

# Просмотр базы данных
npm run db:studio

# Сброс базы данных
npx prisma migrate reset

# Создание новой миграции
npx prisma migrate dev --name migration_name
```

## ⚠️ Важные заметки

1. **Переменные окружения**: Обязательно настройте все переменные в `.env.development.local`
2. **База данных**: PostgreSQL должен быть запущен перед стартом приложения
3. **MEGA**: Для тестирования загрузки файлов нужны валидные MEGA креды
4. **SMTP**: Для тестирования email используйте Mailtrap или другой тестовый SMTP

## 🆘 Решение проблем

### Ошибки установки зависимостей
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
```

### Ошибки базы данных
```bash
# Проверьте подключение к PostgreSQL
npx prisma db pull

# Сбросьте и пересоздайте базу
npx prisma migrate reset
npm run db:seed
```

### Ошибки компиляции TypeScript
Все ошибки TypeScript связанные с отсутствующими модулями исчезнут после `npm install`.

---

**Готово!** Приложение должно работать на http://localhost:3000 🎉
